import 'package:flutter/material.dart';
class AdminHomeTap extends StatelessWidget {
  const AdminHomeTap({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Admin'),
    );
  }
}
